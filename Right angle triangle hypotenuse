import math

length = float(input("Enter the length of the right angle triangle: "))
width = float(input("Enter the width of the right angle triangle: "))

hypotenuse = math.sqrt(length ** 2 + width ** 2)

print(f"The hypotenuse of your right angle triangle is {round(hypotenuse, 2)}cm long")
